# Outputs layout

Runtime outputs are intentionally ignored by Git. Expected experiment folders:

- `outputs/base`
- `outputs/exp_a`
- `outputs/exp_b`

Each benchmark run writes predictions, metrics, `environment.json`, `benchmark_summary.json`, and `summary.md`.
